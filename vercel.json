{
   "version": 2,
   "name": "zerokataserver",
   "builds": [
      { "src": "./backend/server.js", "use": "@vercel/node" }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "./backend/server.js" }
   ]
}
